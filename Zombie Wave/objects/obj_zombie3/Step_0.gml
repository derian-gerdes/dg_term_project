/// @description Insert description here
// You can write your code in this editor

checkForPlayer();

//Update points and destroy zombie
if (hp) <= 0
{
    with(obj_points) thepoints = thepoints + 25;
    instance_destroy();
}